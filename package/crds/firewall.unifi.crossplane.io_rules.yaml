---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: rules.firewall.unifi.crossplane.io
spec:
  group: firewall.unifi.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - unifi
    kind: Rule
    listKind: RuleList
    plural: rules
    singular: rule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Rule is the Schema for the Rules API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RuleSpec defines the desired state of Rule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  action:
                    description: The action of the firewall rule. Must be one of `drop`,
                      `accept`, or `reject`.
                    type: string
                  dstAddress:
                    description: The destination address of the firewall rule.
                    type: string
                  dstAddressIpv6:
                    description: The IPv6 destination address of the firewall rule.
                    type: string
                  dstFirewallGroupIds:
                    description: The destination firewall group IDs of the firewall
                      rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dstNetworkId:
                    description: The destination network ID of the firewall rule.
                    type: string
                  dstNetworkType:
                    description: The destination network type of the firewall rule.
                      Can be one of `ADDRv4` or `NETv4`.
                    type: string
                  dstPort:
                    description: The destination port of the firewall rule.
                    type: string
                  enabled:
                    description: Specifies whether the rule should be enabled.
                    type: boolean
                  icmpTypename:
                    description: ICMP type name.
                    type: string
                  icmpV6Typename:
                    description: ICMPv6 type name.
                    type: string
                  ipSec:
                    description: Specify whether the rule matches on IPsec packets.
                      Can be one of `match-ipset` or `match-none`.
                    type: string
                  logging:
                    description: Enable logging for the firewall rule.
                    type: boolean
                  name:
                    description: The name of the firewall rule.
                    type: string
                  protocol:
                    description: The protocol of the rule.
                    type: string
                  protocolMatchExcepted:
                    description: Match packets that do NOT match the specified protocol
                      (protocol negation).
                    type: boolean
                  protocolV6:
                    description: The IPv6 protocol of the rule.
                    type: string
                  ruleIndex:
                    description: 'The index of the rule. Must be in one of the interface-specific
                      blocks: `2000-2999` (LAN), `3000-3999` (WAN), `4000-4999` (GUEST),
                      or their high-range equivalents `20000-29999`, `30000-39999`,
                      `40000-49999` used by newer UniFi OS versions.'
                    type: number
                  ruleset:
                    description: The ruleset for the rule. This is from the perspective
                      of the security gateway.
                    type: string
                  settingPreference:
                    description: Whether the rule is managed automatically by the
                      controller or manually. Can be one of `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the firewall rule
                      with.
                    type: string
                  srcAddress:
                    description: The source address for the firewall rule.
                    type: string
                  srcAddressIpv6:
                    description: The IPv6 source address for the firewall rule.
                    type: string
                  srcFirewallGroupIds:
                    description: The source firewall group IDs for the firewall rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  srcMac:
                    description: The source MAC address of the firewall rule.
                    type: string
                  srcNetworkId:
                    description: The source network ID for the firewall rule.
                    type: string
                  srcNetworkType:
                    description: The source network type of the firewall rule. Can
                      be one of `ADDRv4` or `NETv4`.
                    type: string
                  srcPort:
                    description: The source port of the firewall rule.
                    type: string
                  stateEstablished:
                    description: Match where the state is established.
                    type: boolean
                  stateInvalid:
                    description: Match where the state is invalid.
                    type: boolean
                  stateNew:
                    description: Match where the state is new.
                    type: boolean
                  stateRelated:
                    description: Match where the state is related.
                    type: boolean
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: The action of the firewall rule. Must be one of `drop`,
                      `accept`, or `reject`.
                    type: string
                  dstAddress:
                    description: The destination address of the firewall rule.
                    type: string
                  dstAddressIpv6:
                    description: The IPv6 destination address of the firewall rule.
                    type: string
                  dstFirewallGroupIds:
                    description: The destination firewall group IDs of the firewall
                      rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dstNetworkId:
                    description: The destination network ID of the firewall rule.
                    type: string
                  dstNetworkType:
                    description: The destination network type of the firewall rule.
                      Can be one of `ADDRv4` or `NETv4`.
                    type: string
                  dstPort:
                    description: The destination port of the firewall rule.
                    type: string
                  enabled:
                    description: Specifies whether the rule should be enabled.
                    type: boolean
                  icmpTypename:
                    description: ICMP type name.
                    type: string
                  icmpV6Typename:
                    description: ICMPv6 type name.
                    type: string
                  ipSec:
                    description: Specify whether the rule matches on IPsec packets.
                      Can be one of `match-ipset` or `match-none`.
                    type: string
                  logging:
                    description: Enable logging for the firewall rule.
                    type: boolean
                  name:
                    description: The name of the firewall rule.
                    type: string
                  protocol:
                    description: The protocol of the rule.
                    type: string
                  protocolMatchExcepted:
                    description: Match packets that do NOT match the specified protocol
                      (protocol negation).
                    type: boolean
                  protocolV6:
                    description: The IPv6 protocol of the rule.
                    type: string
                  ruleIndex:
                    description: 'The index of the rule. Must be in one of the interface-specific
                      blocks: `2000-2999` (LAN), `3000-3999` (WAN), `4000-4999` (GUEST),
                      or their high-range equivalents `20000-29999`, `30000-39999`,
                      `40000-49999` used by newer UniFi OS versions.'
                    type: number
                  ruleset:
                    description: The ruleset for the rule. This is from the perspective
                      of the security gateway.
                    type: string
                  settingPreference:
                    description: Whether the rule is managed automatically by the
                      controller or manually. Can be one of `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the firewall rule
                      with.
                    type: string
                  srcAddress:
                    description: The source address for the firewall rule.
                    type: string
                  srcAddressIpv6:
                    description: The IPv6 source address for the firewall rule.
                    type: string
                  srcFirewallGroupIds:
                    description: The source firewall group IDs for the firewall rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  srcMac:
                    description: The source MAC address of the firewall rule.
                    type: string
                  srcNetworkId:
                    description: The source network ID for the firewall rule.
                    type: string
                  srcNetworkType:
                    description: The source network type of the firewall rule. Can
                      be one of `ADDRv4` or `NETv4`.
                    type: string
                  srcPort:
                    description: The source port of the firewall rule.
                    type: string
                  stateEstablished:
                    description: Match where the state is established.
                    type: boolean
                  stateInvalid:
                    description: Match where the state is invalid.
                    type: boolean
                  stateNew:
                    description: Match where the state is new.
                    type: boolean
                  stateRelated:
                    description: Match where the state is related.
                    type: boolean
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.ruleIndex is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleIndex)
                || (has(self.initProvider) && has(self.initProvider.ruleIndex))'
            - message: spec.forProvider.ruleset is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.ruleset)
                || (has(self.initProvider) && has(self.initProvider.ruleset))'
          status:
            description: RuleStatus defines the observed state of Rule.
            properties:
              atProvider:
                properties:
                  action:
                    description: The action of the firewall rule. Must be one of `drop`,
                      `accept`, or `reject`.
                    type: string
                  dstAddress:
                    description: The destination address of the firewall rule.
                    type: string
                  dstAddressIpv6:
                    description: The IPv6 destination address of the firewall rule.
                    type: string
                  dstFirewallGroupIds:
                    description: The destination firewall group IDs of the firewall
                      rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  dstNetworkId:
                    description: The destination network ID of the firewall rule.
                    type: string
                  dstNetworkType:
                    description: The destination network type of the firewall rule.
                      Can be one of `ADDRv4` or `NETv4`.
                    type: string
                  dstPort:
                    description: The destination port of the firewall rule.
                    type: string
                  enabled:
                    description: Specifies whether the rule should be enabled.
                    type: boolean
                  icmpTypename:
                    description: ICMP type name.
                    type: string
                  icmpV6Typename:
                    description: ICMPv6 type name.
                    type: string
                  id:
                    type: string
                  ipSec:
                    description: Specify whether the rule matches on IPsec packets.
                      Can be one of `match-ipset` or `match-none`.
                    type: string
                  logging:
                    description: Enable logging for the firewall rule.
                    type: boolean
                  name:
                    description: The name of the firewall rule.
                    type: string
                  protocol:
                    description: The protocol of the rule.
                    type: string
                  protocolMatchExcepted:
                    description: Match packets that do NOT match the specified protocol
                      (protocol negation).
                    type: boolean
                  protocolV6:
                    description: The IPv6 protocol of the rule.
                    type: string
                  ruleIndex:
                    description: 'The index of the rule. Must be in one of the interface-specific
                      blocks: `2000-2999` (LAN), `3000-3999` (WAN), `4000-4999` (GUEST),
                      or their high-range equivalents `20000-29999`, `30000-39999`,
                      `40000-49999` used by newer UniFi OS versions.'
                    type: number
                  ruleset:
                    description: The ruleset for the rule. This is from the perspective
                      of the security gateway.
                    type: string
                  settingPreference:
                    description: Whether the rule is managed automatically by the
                      controller or manually. Can be one of `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the firewall rule
                      with.
                    type: string
                  srcAddress:
                    description: The source address for the firewall rule.
                    type: string
                  srcAddressIpv6:
                    description: The IPv6 source address for the firewall rule.
                    type: string
                  srcFirewallGroupIds:
                    description: The source firewall group IDs for the firewall rule.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  srcMac:
                    description: The source MAC address of the firewall rule.
                    type: string
                  srcNetworkId:
                    description: The source network ID for the firewall rule.
                    type: string
                  srcNetworkType:
                    description: The source network type of the firewall rule. Can
                      be one of `ADDRv4` or `NETv4`.
                    type: string
                  srcPort:
                    description: The source port of the firewall rule.
                    type: string
                  stateEstablished:
                    description: Match where the state is established.
                    type: boolean
                  stateInvalid:
                    description: Match where the state is invalid.
                    type: boolean
                  stateNew:
                    description: Match where the state is new.
                    type: boolean
                  stateRelated:
                    description: Match where the state is related.
                    type: boolean
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
