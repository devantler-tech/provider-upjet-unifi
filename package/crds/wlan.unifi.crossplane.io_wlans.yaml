---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: wlans.wlan.unifi.crossplane.io
spec:
  group: wlan.unifi.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - unifi
    kind: Wlan
    listKind: WlanList
    plural: wlans
    singular: wlan
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Wlan is the Schema for the Wlans API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WlanSpec defines the desired state of Wlan
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  apGroupIds:
                    description: List of AP group IDs to apply this WLAN to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  apGroupMode:
                    description: Access point group mode.
                    type: string
                  bcFilterList:
                    description: List of MAC addresses for the broadcast filter. The
                      controller may populate this on its own, so it is computed when
                      unset.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bssTransition:
                    description: Improves client roaming by providing connection details
                      of nearby APs.
                    type: boolean
                  dtim6E:
                    description: DTIM period for the 6 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimMode:
                    description: DTIM mode. Can be one of `default` or `custom`. Use
                      `custom` together with `dtim_ng`/`dtim_na`/`dtim_6e`.
                    type: string
                  dtimNa:
                    description: DTIM period for the 5 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimNg:
                    description: DTIM period for the 2.4 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  enabled:
                    description: Enable or disable the WLAN.
                    type: boolean
                  enhancedIot:
                    description: Enable enhanced IoT connectivity. When `true`, the
                      controller forces `iapp_enabled = true`, `wpa3_support = false`,
                      `wpa3_transition = false`, `pmf_mode = "disabled"` and `dtim_ng
                      = 1`; the provider pins those fields to match, so any conflicting
                      values you set for them are ignored (this disables WPA3 on the
                      SSID).
                    type: boolean
                  fastRoamingEnabled:
                    description: Enable fast roaming, aka 802.11r.
                    type: boolean
                  groupRekey:
                    description: Group rekey interval in seconds (0 to disable).
                    type: number
                  hideSsid:
                    description: Indicates whether or not to hide the SSID from broadcast.
                    type: boolean
                  hotspot2confEnabled:
                    description: Enable Hotspot 2.0 configuration.
                    type: boolean
                  iappEnabled:
                    description: Enable Inter-Access Point Protocol (802.11f) for
                      faster roaming. Computed from the controller when not set.
                    type: boolean
                  isGuest:
                    description: Indicates that this is a guest WLAN and should use
                      guest behaviors.
                    type: boolean
                  l2Isolation:
                    description: Isolates stations on layer 2 (ethernet) level.
                    type: boolean
                  macFilter:
                    properties:
                      enabled:
                        description: Indicates whether or not the MAC filter is turned
                          on for the network.
                        type: boolean
                      list:
                        description: List of MAC addresses to filter (only valid if
                          `enabled` is `true`).
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      policy:
                        description: MAC address filter policy (only valid if `enabled`
                          is `true`).
                        type: string
                    type: object
                  minimumDataRate2GKbps:
                    description: Minimum data rate for 2G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `1000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minimumDataRate5GKbps:
                    description: Minimum data rate for 5G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `6000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minrateSettingPreference:
                    description: Minimum rate setting preference.
                    type: string
                  mloEnabled:
                    description: Enable Multi-Link Operation (6 GHz).
                    type: boolean
                  multicastEnhance:
                    description: Indicates whether or not Multicast Enhance is turned
                      of for the network.
                    type: boolean
                  name:
                    description: The SSID of the network.
                    type: string
                  nasIdentifierType:
                    description: NAS identifier type for RADIUS.
                    type: string
                  networkId:
                    description: ID of the network for this WLAN.
                    type: string
                  no2ghzOui:
                    description: Connect high performance clients to 5 GHz only.
                    type: boolean
                  passphraseSecretRef:
                    description: The passphrase for the network, only required if
                      `security` is not `open`. Stored in state — use `passphrase_wo`
                      to avoid persisting the secret.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  passphraseWoSecretRef:
                    description: 11+). Used at apply time but never written to state,
                      so it can be sourced from an ephemeral resource (e.g. a Vault
                      secret). Mutually exclusive with `passphrase`.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  pmfMode:
                    description: Enable Protected Management Frames. This cannot be
                      disabled if using WPA 3.
                    type: string
                  privatePresharedKeys:
                    items:
                      properties:
                        networkId:
                          description: ID of the network/VLAN this key is bound to.
                            Leave unset to use the WLAN's default network.
                          type: string
                        passwordSecretRef:
                          description: The passphrase for this key (8-255 characters).
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      type: object
                    type: array
                  privatePresharedKeysEnabled:
                    description: Whether per-key (PPSK) passphrases are enabled for
                      this WLAN. Requires `security = wpapsk`.
                    type: boolean
                  proxyArp:
                    description: Reduces airtime usage by allowing APs to "proxy"
                      common broadcast frames as unicast.
                    type: boolean
                  radiusMacAuthEnabled:
                    description: Enable RADIUS MAC authentication.
                    type: boolean
                  radiusProfileId:
                    description: ID of the RADIUS profile to use when security `wpaeap`.
                      The controller may assign a default profile, so this is computed
                      when unset.
                    type: string
                  schedule:
                    description: Start and stop schedules for the WLAN
                    items:
                      properties:
                        dayOfWeek:
                          description: Day of week for the block.
                          type: string
                        duration:
                          description: Length of the block, as a Go duration string.
                            The controller stores this value with one-minute resolution,
                            so the duration must be at least `1m` and a whole multiple
                            of one minute (e.g. `30m`, `2h`).
                          type: string
                        name:
                          description: Name of the block.
                          type: string
                        startHour:
                          description: Start hour for the block (0-23).
                          type: number
                        startMinute:
                          description: Start minute for the block (0-59).
                          type: number
                      type: object
                    type: array
                  security:
                    description: The type of WiFi security for this network.
                    type: string
                  site:
                    description: The name of the site to associate the WLAN with.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  uapsd:
                    description: Enable Unscheduled Automatic Power Save Delivery.
                    type: boolean
                  userGroupId:
                    description: ID of the user group to use for this network.
                    type: string
                  vlan:
                    description: VLAN ID.
                    type: number
                  vlanEnabled:
                    description: Enable VLAN tagging.
                    type: boolean
                  wlanBand:
                    description: WLAN band.
                    type: string
                  wlanBands:
                    description: List of WLAN bands.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  wpa3Enhanced192:
                    description: Enable WPA3 Enterprise 192-bit mode.
                    type: boolean
                  wpa3FastRoaming:
                    description: Enable WPA3 fast roaming (802.11r).
                    type: boolean
                  wpa3Support:
                    description: Enable WPA 3 support (security must be `wpapsk` and
                      PMF must be turned on).
                    type: boolean
                  wpa3Transition:
                    description: Enable WPA 3 and WPA 2 support (security must be
                      `wpapsk` and `wpa3_support` must be true).
                    type: boolean
                  wpaEnc:
                    description: WPA encryption. Can be one of `auto`, `ccmp`, `gcmp`,
                      `ccmp-256`, or `gcmp-256`.
                    type: string
                  wpaMode:
                    description: WPA mode. Can be one of `auto`, `wpa1`, or `wpa2`.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  apGroupIds:
                    description: List of AP group IDs to apply this WLAN to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  apGroupMode:
                    description: Access point group mode.
                    type: string
                  bcFilterList:
                    description: List of MAC addresses for the broadcast filter. The
                      controller may populate this on its own, so it is computed when
                      unset.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bssTransition:
                    description: Improves client roaming by providing connection details
                      of nearby APs.
                    type: boolean
                  dtim6E:
                    description: DTIM period for the 6 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimMode:
                    description: DTIM mode. Can be one of `default` or `custom`. Use
                      `custom` together with `dtim_ng`/`dtim_na`/`dtim_6e`.
                    type: string
                  dtimNa:
                    description: DTIM period for the 5 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimNg:
                    description: DTIM period for the 2.4 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  enabled:
                    description: Enable or disable the WLAN.
                    type: boolean
                  enhancedIot:
                    description: Enable enhanced IoT connectivity. When `true`, the
                      controller forces `iapp_enabled = true`, `wpa3_support = false`,
                      `wpa3_transition = false`, `pmf_mode = "disabled"` and `dtim_ng
                      = 1`; the provider pins those fields to match, so any conflicting
                      values you set for them are ignored (this disables WPA3 on the
                      SSID).
                    type: boolean
                  fastRoamingEnabled:
                    description: Enable fast roaming, aka 802.11r.
                    type: boolean
                  groupRekey:
                    description: Group rekey interval in seconds (0 to disable).
                    type: number
                  hideSsid:
                    description: Indicates whether or not to hide the SSID from broadcast.
                    type: boolean
                  hotspot2confEnabled:
                    description: Enable Hotspot 2.0 configuration.
                    type: boolean
                  iappEnabled:
                    description: Enable Inter-Access Point Protocol (802.11f) for
                      faster roaming. Computed from the controller when not set.
                    type: boolean
                  isGuest:
                    description: Indicates that this is a guest WLAN and should use
                      guest behaviors.
                    type: boolean
                  l2Isolation:
                    description: Isolates stations on layer 2 (ethernet) level.
                    type: boolean
                  macFilter:
                    properties:
                      enabled:
                        description: Indicates whether or not the MAC filter is turned
                          on for the network.
                        type: boolean
                      list:
                        description: List of MAC addresses to filter (only valid if
                          `enabled` is `true`).
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      policy:
                        description: MAC address filter policy (only valid if `enabled`
                          is `true`).
                        type: string
                    type: object
                  minimumDataRate2GKbps:
                    description: Minimum data rate for 2G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `1000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minimumDataRate5GKbps:
                    description: Minimum data rate for 5G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `6000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minrateSettingPreference:
                    description: Minimum rate setting preference.
                    type: string
                  mloEnabled:
                    description: Enable Multi-Link Operation (6 GHz).
                    type: boolean
                  multicastEnhance:
                    description: Indicates whether or not Multicast Enhance is turned
                      of for the network.
                    type: boolean
                  name:
                    description: The SSID of the network.
                    type: string
                  nasIdentifierType:
                    description: NAS identifier type for RADIUS.
                    type: string
                  networkId:
                    description: ID of the network for this WLAN.
                    type: string
                  no2ghzOui:
                    description: Connect high performance clients to 5 GHz only.
                    type: boolean
                  passphraseSecretRef:
                    description: The passphrase for the network, only required if
                      `security` is not `open`. Stored in state — use `passphrase_wo`
                      to avoid persisting the secret.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  passphraseWoSecretRef:
                    description: 11+). Used at apply time but never written to state,
                      so it can be sourced from an ephemeral resource (e.g. a Vault
                      secret). Mutually exclusive with `passphrase`.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  pmfMode:
                    description: Enable Protected Management Frames. This cannot be
                      disabled if using WPA 3.
                    type: string
                  privatePresharedKeys:
                    items:
                      properties:
                        networkId:
                          description: ID of the network/VLAN this key is bound to.
                            Leave unset to use the WLAN's default network.
                          type: string
                        passwordSecretRef:
                          description: The passphrase for this key (8-255 characters).
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                            namespace:
                              description: Namespace of the secret.
                              type: string
                          required:
                          - key
                          - name
                          - namespace
                          type: object
                      required:
                      - passwordSecretRef
                      type: object
                    type: array
                  privatePresharedKeysEnabled:
                    description: Whether per-key (PPSK) passphrases are enabled for
                      this WLAN. Requires `security = wpapsk`.
                    type: boolean
                  proxyArp:
                    description: Reduces airtime usage by allowing APs to "proxy"
                      common broadcast frames as unicast.
                    type: boolean
                  radiusMacAuthEnabled:
                    description: Enable RADIUS MAC authentication.
                    type: boolean
                  radiusProfileId:
                    description: ID of the RADIUS profile to use when security `wpaeap`.
                      The controller may assign a default profile, so this is computed
                      when unset.
                    type: string
                  schedule:
                    description: Start and stop schedules for the WLAN
                    items:
                      properties:
                        dayOfWeek:
                          description: Day of week for the block.
                          type: string
                        duration:
                          description: Length of the block, as a Go duration string.
                            The controller stores this value with one-minute resolution,
                            so the duration must be at least `1m` and a whole multiple
                            of one minute (e.g. `30m`, `2h`).
                          type: string
                        name:
                          description: Name of the block.
                          type: string
                        startHour:
                          description: Start hour for the block (0-23).
                          type: number
                        startMinute:
                          description: Start minute for the block (0-59).
                          type: number
                      type: object
                    type: array
                  security:
                    description: The type of WiFi security for this network.
                    type: string
                  site:
                    description: The name of the site to associate the WLAN with.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  uapsd:
                    description: Enable Unscheduled Automatic Power Save Delivery.
                    type: boolean
                  userGroupId:
                    description: ID of the user group to use for this network.
                    type: string
                  vlan:
                    description: VLAN ID.
                    type: number
                  vlanEnabled:
                    description: Enable VLAN tagging.
                    type: boolean
                  wlanBand:
                    description: WLAN band.
                    type: string
                  wlanBands:
                    description: List of WLAN bands.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  wpa3Enhanced192:
                    description: Enable WPA3 Enterprise 192-bit mode.
                    type: boolean
                  wpa3FastRoaming:
                    description: Enable WPA3 fast roaming (802.11r).
                    type: boolean
                  wpa3Support:
                    description: Enable WPA 3 support (security must be `wpapsk` and
                      PMF must be turned on).
                    type: boolean
                  wpa3Transition:
                    description: Enable WPA 3 and WPA 2 support (security must be
                      `wpapsk` and `wpa3_support` must be true).
                    type: boolean
                  wpaEnc:
                    description: WPA encryption. Can be one of `auto`, `ccmp`, `gcmp`,
                      `ccmp-256`, or `gcmp-256`.
                    type: string
                  wpaMode:
                    description: WPA mode. Can be one of `auto`, `wpa1`, or `wpa2`.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.security is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.security)
                || (has(self.initProvider) && has(self.initProvider.security))'
            - message: spec.forProvider.userGroupId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.userGroupId)
                || (has(self.initProvider) && has(self.initProvider.userGroupId))'
          status:
            description: WlanStatus defines the observed state of Wlan.
            properties:
              atProvider:
                properties:
                  apGroupIds:
                    description: List of AP group IDs to apply this WLAN to.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  apGroupMode:
                    description: Access point group mode.
                    type: string
                  bcFilterList:
                    description: List of MAC addresses for the broadcast filter. The
                      controller may populate this on its own, so it is computed when
                      unset.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  bssTransition:
                    description: Improves client roaming by providing connection details
                      of nearby APs.
                    type: boolean
                  dtim6E:
                    description: DTIM period for the 6 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimMode:
                    description: DTIM mode. Can be one of `default` or `custom`. Use
                      `custom` together with `dtim_ng`/`dtim_na`/`dtim_6e`.
                    type: string
                  dtimNa:
                    description: DTIM period for the 5 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  dtimNg:
                    description: DTIM period for the 2.4 GHz band (1-255). Only used
                      when `dtim_mode` is `custom`. Computed from the controller when
                      not set.
                    type: number
                  enabled:
                    description: Enable or disable the WLAN.
                    type: boolean
                  enhancedIot:
                    description: Enable enhanced IoT connectivity. When `true`, the
                      controller forces `iapp_enabled = true`, `wpa3_support = false`,
                      `wpa3_transition = false`, `pmf_mode = "disabled"` and `dtim_ng
                      = 1`; the provider pins those fields to match, so any conflicting
                      values you set for them are ignored (this disables WPA3 on the
                      SSID).
                    type: boolean
                  fastRoamingEnabled:
                    description: Enable fast roaming, aka 802.11r.
                    type: boolean
                  groupRekey:
                    description: Group rekey interval in seconds (0 to disable).
                    type: number
                  hideSsid:
                    description: Indicates whether or not to hide the SSID from broadcast.
                    type: boolean
                  hotspot2confEnabled:
                    description: Enable Hotspot 2.0 configuration.
                    type: boolean
                  iappEnabled:
                    description: Enable Inter-Access Point Protocol (802.11f) for
                      faster roaming. Computed from the controller when not set.
                    type: boolean
                  id:
                    type: string
                  isGuest:
                    description: Indicates that this is a guest WLAN and should use
                      guest behaviors.
                    type: boolean
                  l2Isolation:
                    description: Isolates stations on layer 2 (ethernet) level.
                    type: boolean
                  macFilter:
                    properties:
                      enabled:
                        description: Indicates whether or not the MAC filter is turned
                          on for the network.
                        type: boolean
                      list:
                        description: List of MAC addresses to filter (only valid if
                          `enabled` is `true`).
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                      policy:
                        description: MAC address filter policy (only valid if `enabled`
                          is `true`).
                        type: string
                    type: object
                  minimumDataRate2GKbps:
                    description: Minimum data rate for 2G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `1000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minimumDataRate5GKbps:
                    description: Minimum data rate for 5G clients in Kbps. When unset,
                      the controller assigns a value (e.g. `6000` in `auto` mode),
                      so this is computed rather than defaulted to `0`.
                    type: number
                  minrateSettingPreference:
                    description: Minimum rate setting preference.
                    type: string
                  mloEnabled:
                    description: Enable Multi-Link Operation (6 GHz).
                    type: boolean
                  multicastEnhance:
                    description: Indicates whether or not Multicast Enhance is turned
                      of for the network.
                    type: boolean
                  name:
                    description: The SSID of the network.
                    type: string
                  nasIdentifierType:
                    description: NAS identifier type for RADIUS.
                    type: string
                  networkId:
                    description: ID of the network for this WLAN.
                    type: string
                  no2ghzOui:
                    description: Connect high performance clients to 5 GHz only.
                    type: boolean
                  pmfMode:
                    description: Enable Protected Management Frames. This cannot be
                      disabled if using WPA 3.
                    type: string
                  privatePresharedKeys:
                    items:
                      properties:
                        networkId:
                          description: ID of the network/VLAN this key is bound to.
                            Leave unset to use the WLAN's default network.
                          type: string
                      type: object
                    type: array
                  privatePresharedKeysEnabled:
                    description: Whether per-key (PPSK) passphrases are enabled for
                      this WLAN. Requires `security = wpapsk`.
                    type: boolean
                  proxyArp:
                    description: Reduces airtime usage by allowing APs to "proxy"
                      common broadcast frames as unicast.
                    type: boolean
                  radiusMacAuthEnabled:
                    description: Enable RADIUS MAC authentication.
                    type: boolean
                  radiusProfileId:
                    description: ID of the RADIUS profile to use when security `wpaeap`.
                      The controller may assign a default profile, so this is computed
                      when unset.
                    type: string
                  schedule:
                    description: Start and stop schedules for the WLAN
                    items:
                      properties:
                        dayOfWeek:
                          description: Day of week for the block.
                          type: string
                        duration:
                          description: Length of the block, as a Go duration string.
                            The controller stores this value with one-minute resolution,
                            so the duration must be at least `1m` and a whole multiple
                            of one minute (e.g. `30m`, `2h`).
                          type: string
                        name:
                          description: Name of the block.
                          type: string
                        startHour:
                          description: Start hour for the block (0-23).
                          type: number
                        startMinute:
                          description: Start minute for the block (0-59).
                          type: number
                      type: object
                    type: array
                  security:
                    description: The type of WiFi security for this network.
                    type: string
                  site:
                    description: The name of the site to associate the WLAN with.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  uapsd:
                    description: Enable Unscheduled Automatic Power Save Delivery.
                    type: boolean
                  userGroupId:
                    description: ID of the user group to use for this network.
                    type: string
                  vlan:
                    description: VLAN ID.
                    type: number
                  vlanEnabled:
                    description: Enable VLAN tagging.
                    type: boolean
                  wlanBand:
                    description: WLAN band.
                    type: string
                  wlanBands:
                    description: List of WLAN bands.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  wpa3Enhanced192:
                    description: Enable WPA3 Enterprise 192-bit mode.
                    type: boolean
                  wpa3FastRoaming:
                    description: Enable WPA3 fast roaming (802.11r).
                    type: boolean
                  wpa3Support:
                    description: Enable WPA 3 support (security must be `wpapsk` and
                      PMF must be turned on).
                    type: boolean
                  wpa3Transition:
                    description: Enable WPA 3 and WPA 2 support (security must be
                      `wpapsk` and `wpa3_support` must be true).
                    type: boolean
                  wpaEnc:
                    description: WPA encryption. Can be one of `auto`, `ccmp`, `gcmp`,
                      `ccmp-256`, or `gcmp-256`.
                    type: string
                  wpaMode:
                    description: WPA mode. Can be one of `auto`, `wpa1`, or `wpa2`.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
