---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: profiles.port.unifi.m.crossplane.io
spec:
  group: port.unifi.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - unifi
    kind: Profile
    listKind: ProfileList
    plural: profiles
    singular: profile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Profile is the Schema for the Profiles API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProfileSpec defines the desired state of Profile
            properties:
              forProvider:
                properties:
                  autoneg:
                    description: Enable link auto negotiation for the port profile.
                      When set to `true` this overrides `speed`.
                    type: boolean
                  dot1xCtrl:
                    description: The type of 802.1X control to use. Can be `auto`,
                      `force_authorized`, `force_unauthorized`, `mac_based` or `multi_host`.
                    type: string
                  dot1xIdleTimeout:
                    description: The idle timeout to use when using MAC Based 802.1X
                      control, as a Go duration string (e.g. `5m`, `300s`). Defaults
                      to `5m0s`.
                    type: string
                  egressRateLimitKbps:
                    description: The egress rate limit, in kpbs, for the port profile.
                      Can be between `64` and `9999999`.
                    type: number
                  egressRateLimitKbpsEnabled:
                    description: Enable egress rate limiting for the port profile.
                    type: boolean
                  excludedNetworkconfIds:
                    description: The IDs of networks excluded from the port profile
                      (used when `tagged_vlan_mgmt` is `custom`). Computed from the
                      controller when not set.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fecMode:
                    description: Forward Error Correction mode. Can be `rs-fec`, `fc-fec`,
                      `default`, or `disabled`.
                    type: string
                  forward:
                    description: The type forwarding to use for the port profile.
                      Can be `all`, `native`, `customize` or `disabled`.
                    type: string
                  fullDuplex:
                    description: Enable full duplex for the port profile.
                    type: boolean
                  isolation:
                    description: Enable port isolation for the port profile.
                    type: boolean
                  lldpmedEnabled:
                    description: Enable LLDP-MED for the port profile.
                    type: boolean
                  lldpmedNotifyEnabled:
                    description: Enable LLDP-MED topology change notifications for
                      the port profile.
                    type: boolean
                  multicastRouterNetworkconfIds:
                    description: The IDs of networks designated as multicast routers
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the port profile.
                    type: string
                  nativeNetworkconfId:
                    description: The ID of network to use as the main (native/untagged)
                      network on the port profile. Assigned by the controller if not
                      set.
                    type: string
                  opMode:
                    description: The operation mode for the port profile. Can only
                      be `switch`
                    type: string
                  poeMode:
                    description: The POE mode for the port profile. Can be one of
                      `auto`, `passv24`, `passthrough` or `off`.
                    type: string
                  portKeepaliveEnabled:
                    description: Enable port keepalive for the port profile.
                    type: boolean
                  portSecurityEnabled:
                    description: Enable port security for the port profile.
                    type: boolean
                  portSecurityMacAddress:
                    description: The MAC addresses associated with the port security
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  priorityQueue1Level:
                    description: The priority queue 1 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue2Level:
                    description: The priority queue 2 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue3Level:
                    description: The priority queue 3 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue4Level:
                    description: The priority queue 4 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  settingPreference:
                    description: Whether the port profile settings are managed automatically
                      or manually. Can be `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the port profile
                      with.
                    type: string
                  speed:
                    description: The link speed to set for the port profile. Can be
                      one of `10`, `100`, `1000`, `2500`, `5000`, `10000`, `20000`,
                      `25000`, `40000`, `50000` or `100000`
                    type: number
                  stormctrlBcastEnabled:
                    description: Enable broadcast Storm Control for the port profile.
                    type: boolean
                  stormctrlBcastLevel:
                    description: The broadcast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlBcastRate:
                    description: The broadcast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlMcastEnabled:
                    description: Enable multicast Storm Control for the port profile.
                    type: boolean
                  stormctrlMcastLevel:
                    description: The multicast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlMcastRate:
                    description: The multicast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlType:
                    description: The type of Storm Control to use for the port profile.
                      Can be `level` or `rate`.
                    type: string
                  stormctrlUcastEnabled:
                    description: Enable unknown unicast Storm Control for the port
                      profile.
                    type: boolean
                  stormctrlUcastLevel:
                    description: The unknown unicast Storm Control level for the port
                      profile. Can be between 0 and 100.
                    type: number
                  stormctrlUcastRate:
                    description: The unknown unicast Storm Control rate for the port
                      profile. Can be between 0 and 14880000.
                    type: number
                  stpPortMode:
                    description: Enable Spanning Tree Protocol (STP) for the port
                      profile. Computed from the controller when not set.
                    type: boolean
                  taggedNetworkconfIds:
                    description: The IDs of networks to tag traffic with for the port
                      profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  taggedVlanMgmt:
                    description: How tagged VLANs are managed on the port. Can be
                      `auto`, `block_all`, or `custom`.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  voiceNetworkconfId:
                    description: The ID of network to use for voice traffic for the
                      port profile.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoneg:
                    description: Enable link auto negotiation for the port profile.
                      When set to `true` this overrides `speed`.
                    type: boolean
                  dot1xCtrl:
                    description: The type of 802.1X control to use. Can be `auto`,
                      `force_authorized`, `force_unauthorized`, `mac_based` or `multi_host`.
                    type: string
                  dot1xIdleTimeout:
                    description: The idle timeout to use when using MAC Based 802.1X
                      control, as a Go duration string (e.g. `5m`, `300s`). Defaults
                      to `5m0s`.
                    type: string
                  egressRateLimitKbps:
                    description: The egress rate limit, in kpbs, for the port profile.
                      Can be between `64` and `9999999`.
                    type: number
                  egressRateLimitKbpsEnabled:
                    description: Enable egress rate limiting for the port profile.
                    type: boolean
                  excludedNetworkconfIds:
                    description: The IDs of networks excluded from the port profile
                      (used when `tagged_vlan_mgmt` is `custom`). Computed from the
                      controller when not set.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fecMode:
                    description: Forward Error Correction mode. Can be `rs-fec`, `fc-fec`,
                      `default`, or `disabled`.
                    type: string
                  forward:
                    description: The type forwarding to use for the port profile.
                      Can be `all`, `native`, `customize` or `disabled`.
                    type: string
                  fullDuplex:
                    description: Enable full duplex for the port profile.
                    type: boolean
                  isolation:
                    description: Enable port isolation for the port profile.
                    type: boolean
                  lldpmedEnabled:
                    description: Enable LLDP-MED for the port profile.
                    type: boolean
                  lldpmedNotifyEnabled:
                    description: Enable LLDP-MED topology change notifications for
                      the port profile.
                    type: boolean
                  multicastRouterNetworkconfIds:
                    description: The IDs of networks designated as multicast routers
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the port profile.
                    type: string
                  nativeNetworkconfId:
                    description: The ID of network to use as the main (native/untagged)
                      network on the port profile. Assigned by the controller if not
                      set.
                    type: string
                  opMode:
                    description: The operation mode for the port profile. Can only
                      be `switch`
                    type: string
                  poeMode:
                    description: The POE mode for the port profile. Can be one of
                      `auto`, `passv24`, `passthrough` or `off`.
                    type: string
                  portKeepaliveEnabled:
                    description: Enable port keepalive for the port profile.
                    type: boolean
                  portSecurityEnabled:
                    description: Enable port security for the port profile.
                    type: boolean
                  portSecurityMacAddress:
                    description: The MAC addresses associated with the port security
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  priorityQueue1Level:
                    description: The priority queue 1 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue2Level:
                    description: The priority queue 2 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue3Level:
                    description: The priority queue 3 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue4Level:
                    description: The priority queue 4 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  settingPreference:
                    description: Whether the port profile settings are managed automatically
                      or manually. Can be `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the port profile
                      with.
                    type: string
                  speed:
                    description: The link speed to set for the port profile. Can be
                      one of `10`, `100`, `1000`, `2500`, `5000`, `10000`, `20000`,
                      `25000`, `40000`, `50000` or `100000`
                    type: number
                  stormctrlBcastEnabled:
                    description: Enable broadcast Storm Control for the port profile.
                    type: boolean
                  stormctrlBcastLevel:
                    description: The broadcast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlBcastRate:
                    description: The broadcast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlMcastEnabled:
                    description: Enable multicast Storm Control for the port profile.
                    type: boolean
                  stormctrlMcastLevel:
                    description: The multicast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlMcastRate:
                    description: The multicast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlType:
                    description: The type of Storm Control to use for the port profile.
                      Can be `level` or `rate`.
                    type: string
                  stormctrlUcastEnabled:
                    description: Enable unknown unicast Storm Control for the port
                      profile.
                    type: boolean
                  stormctrlUcastLevel:
                    description: The unknown unicast Storm Control level for the port
                      profile. Can be between 0 and 100.
                    type: number
                  stormctrlUcastRate:
                    description: The unknown unicast Storm Control rate for the port
                      profile. Can be between 0 and 14880000.
                    type: number
                  stpPortMode:
                    description: Enable Spanning Tree Protocol (STP) for the port
                      profile. Computed from the controller when not set.
                    type: boolean
                  taggedNetworkconfIds:
                    description: The IDs of networks to tag traffic with for the port
                      profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  taggedVlanMgmt:
                    description: How tagged VLANs are managed on the port. Can be
                      `auto`, `block_all`, or `custom`.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  voiceNetworkconfId:
                    description: The ID of network to use for voice traffic for the
                      port profile.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: ProfileStatus defines the observed state of Profile.
            properties:
              atProvider:
                properties:
                  autoneg:
                    description: Enable link auto negotiation for the port profile.
                      When set to `true` this overrides `speed`.
                    type: boolean
                  dot1xCtrl:
                    description: The type of 802.1X control to use. Can be `auto`,
                      `force_authorized`, `force_unauthorized`, `mac_based` or `multi_host`.
                    type: string
                  dot1xIdleTimeout:
                    description: The idle timeout to use when using MAC Based 802.1X
                      control, as a Go duration string (e.g. `5m`, `300s`). Defaults
                      to `5m0s`.
                    type: string
                  egressRateLimitKbps:
                    description: The egress rate limit, in kpbs, for the port profile.
                      Can be between `64` and `9999999`.
                    type: number
                  egressRateLimitKbpsEnabled:
                    description: Enable egress rate limiting for the port profile.
                    type: boolean
                  excludedNetworkconfIds:
                    description: The IDs of networks excluded from the port profile
                      (used when `tagged_vlan_mgmt` is `custom`). Computed from the
                      controller when not set.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  fecMode:
                    description: Forward Error Correction mode. Can be `rs-fec`, `fc-fec`,
                      `default`, or `disabled`.
                    type: string
                  forward:
                    description: The type forwarding to use for the port profile.
                      Can be `all`, `native`, `customize` or `disabled`.
                    type: string
                  fullDuplex:
                    description: Enable full duplex for the port profile.
                    type: boolean
                  id:
                    type: string
                  isolation:
                    description: Enable port isolation for the port profile.
                    type: boolean
                  lldpmedEnabled:
                    description: Enable LLDP-MED for the port profile.
                    type: boolean
                  lldpmedNotifyEnabled:
                    description: Enable LLDP-MED topology change notifications for
                      the port profile.
                    type: boolean
                  multicastRouterNetworkconfIds:
                    description: The IDs of networks designated as multicast routers
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  name:
                    description: The name of the port profile.
                    type: string
                  nativeNetworkconfId:
                    description: The ID of network to use as the main (native/untagged)
                      network on the port profile. Assigned by the controller if not
                      set.
                    type: string
                  opMode:
                    description: The operation mode for the port profile. Can only
                      be `switch`
                    type: string
                  poeMode:
                    description: The POE mode for the port profile. Can be one of
                      `auto`, `passv24`, `passthrough` or `off`.
                    type: string
                  portKeepaliveEnabled:
                    description: Enable port keepalive for the port profile.
                    type: boolean
                  portSecurityEnabled:
                    description: Enable port security for the port profile.
                    type: boolean
                  portSecurityMacAddress:
                    description: The MAC addresses associated with the port security
                      for the port profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  priorityQueue1Level:
                    description: The priority queue 1 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue2Level:
                    description: The priority queue 2 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue3Level:
                    description: The priority queue 3 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  priorityQueue4Level:
                    description: The priority queue 4 level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  settingPreference:
                    description: Whether the port profile settings are managed automatically
                      or manually. Can be `auto` or `manual`.
                    type: string
                  site:
                    description: The name of the site to associate the port profile
                      with.
                    type: string
                  speed:
                    description: The link speed to set for the port profile. Can be
                      one of `10`, `100`, `1000`, `2500`, `5000`, `10000`, `20000`,
                      `25000`, `40000`, `50000` or `100000`
                    type: number
                  stormctrlBcastEnabled:
                    description: Enable broadcast Storm Control for the port profile.
                    type: boolean
                  stormctrlBcastLevel:
                    description: The broadcast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlBcastRate:
                    description: The broadcast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlMcastEnabled:
                    description: Enable multicast Storm Control for the port profile.
                    type: boolean
                  stormctrlMcastLevel:
                    description: The multicast Storm Control level for the port profile.
                      Can be between 0 and 100.
                    type: number
                  stormctrlMcastRate:
                    description: The multicast Storm Control rate for the port profile.
                      Can be between 0 and 14880000.
                    type: number
                  stormctrlType:
                    description: The type of Storm Control to use for the port profile.
                      Can be `level` or `rate`.
                    type: string
                  stormctrlUcastEnabled:
                    description: Enable unknown unicast Storm Control for the port
                      profile.
                    type: boolean
                  stormctrlUcastLevel:
                    description: The unknown unicast Storm Control level for the port
                      profile. Can be between 0 and 100.
                    type: number
                  stormctrlUcastRate:
                    description: The unknown unicast Storm Control rate for the port
                      profile. Can be between 0 and 14880000.
                    type: number
                  stpPortMode:
                    description: Enable Spanning Tree Protocol (STP) for the port
                      profile. Computed from the controller when not set.
                    type: boolean
                  taggedNetworkconfIds:
                    description: The IDs of networks to tag traffic with for the port
                      profile.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  taggedVlanMgmt:
                    description: How tagged VLANs are managed on the port. Can be
                      `auto`, `block_all`, or `custom`.
                    type: string
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                  voiceNetworkconfId:
                    description: The ID of network to use for voice traffic for the
                      port profile.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
