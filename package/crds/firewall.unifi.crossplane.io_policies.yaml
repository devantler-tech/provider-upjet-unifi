---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: policies.firewall.unifi.crossplane.io
spec:
  group: firewall.unifi.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - unifi
    kind: Policy
    listKind: PolicyList
    plural: policies
    singular: policy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Policy is the Schema for the Policys API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PolicySpec defines the desired state of Policy
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  action:
                    description: 'The action to take when the policy matches: `ALLOW`,
                      `BLOCK`, or `REJECT`.'
                    type: string
                  createAllowRespond:
                    description: When `true`, UniFi automatically creates a matching
                      rule to allow established/related return traffic. Recommended
                      for `ALLOW` policies. Defaults to `false`.
                    type: boolean
                  description:
                    description: A description for the policy.
                    type: string
                  destination:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  enabled:
                    description: Whether the policy is enabled. Defaults to `true`.
                    type: boolean
                  index:
                    description: The ordering index of the policy. UniFi auto-assigns
                      this if not set.
                    type: number
                  ipVersion:
                    description: 'The IP version to match: `BOTH`, `IPV4`, or `IPV6`.
                      Defaults to `IPV4`.'
                    type: string
                  logging:
                    description: Whether to log packets matching this policy. Defaults
                      to `false`.
                    type: boolean
                  name:
                    description: The name of the firewall policy.
                    type: string
                  protocol:
                    description: 'The protocol to match: `all`, `tcp`, `udp`, `tcp_udp`,
                      `icmp`, or `icmpv6`. Defaults to `all`. Note: for `icmp`/`icmpv6`
                      policies the controller rejects `create_allow_respond = true`
                      (`FirewallPolicyCreateRespondTrafficPolicyNotAllowed`) — keep
                      it `false` and add an explicit reverse policy if you need the
                      reply.'
                    type: string
                  site:
                    description: The name of the UniFi site. Defaults to the site
                      configured in the provider.
                    type: string
                  source:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: 'The action to take when the policy matches: `ALLOW`,
                      `BLOCK`, or `REJECT`.'
                    type: string
                  createAllowRespond:
                    description: When `true`, UniFi automatically creates a matching
                      rule to allow established/related return traffic. Recommended
                      for `ALLOW` policies. Defaults to `false`.
                    type: boolean
                  description:
                    description: A description for the policy.
                    type: string
                  destination:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  enabled:
                    description: Whether the policy is enabled. Defaults to `true`.
                    type: boolean
                  index:
                    description: The ordering index of the policy. UniFi auto-assigns
                      this if not set.
                    type: number
                  ipVersion:
                    description: 'The IP version to match: `BOTH`, `IPV4`, or `IPV6`.
                      Defaults to `IPV4`.'
                    type: string
                  logging:
                    description: Whether to log packets matching this policy. Defaults
                      to `false`.
                    type: boolean
                  name:
                    description: The name of the firewall policy.
                    type: string
                  protocol:
                    description: 'The protocol to match: `all`, `tcp`, `udp`, `tcp_udp`,
                      `icmp`, or `icmpv6`. Defaults to `all`. Note: for `icmp`/`icmpv6`
                      policies the controller rejects `create_allow_respond = true`
                      (`FirewallPolicyCreateRespondTrafficPolicyNotAllowed`) — keep
                      it `false` and add an explicit reverse policy if you need the
                      reply.'
                    type: string
                  site:
                    description: The name of the UniFi site. Defaults to the site
                      configured in the provider.
                    type: string
                  source:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.destination is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.destination)
                || (has(self.initProvider) && has(self.initProvider.destination))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.source is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.source)
                || (has(self.initProvider) && has(self.initProvider.source))'
          status:
            description: PolicyStatus defines the observed state of Policy.
            properties:
              atProvider:
                properties:
                  action:
                    description: 'The action to take when the policy matches: `ALLOW`,
                      `BLOCK`, or `REJECT`.'
                    type: string
                  connectionStateType:
                    description: Connection-state matching mode (`ALL`, `RESPOND_ONLY`,
                      or `CUSTOM`). Managed by the UniFi controller; the provider
                      round-trips it so updates are accepted.
                    type: string
                  connectionStates:
                    description: Connection states matched when `connection_state_type`
                      is `CUSTOM` (e.g. `NEW`, `ESTABLISHED`, `RELATED`, `INVALID`).
                      Managed by the UniFi controller; the provider round-trips it
                      so a `CUSTOM` policy's states are not dropped on update (which
                      the firmware rejects with HTTP 400).
                    items:
                      type: string
                    type: array
                  createAllowRespond:
                    description: When `true`, UniFi automatically creates a matching
                      rule to allow established/related return traffic. Recommended
                      for `ALLOW` policies. Defaults to `false`.
                    type: boolean
                  description:
                    description: A description for the policy.
                    type: string
                  destination:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      matchingTargetType:
                        description: How the matching target is specified (`ANY`,
                          `SPECIFIC`, `LIST`, `OBJECT`). Managed by the UniFi controller;
                          the provider round-trips it so updates are accepted.
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  enabled:
                    description: Whether the policy is enabled. Defaults to `true`.
                    type: boolean
                  icmpTypename:
                    description: ICMP type matching mode. Managed by the UniFi controller;
                      the provider round-trips it so updates are accepted.
                    type: string
                  icmpV6Typename:
                    description: ICMPv6 type matching mode. Managed by the UniFi controller;
                      the provider round-trips it so updates are accepted.
                    type: string
                  id:
                    type: string
                  index:
                    description: The ordering index of the policy. UniFi auto-assigns
                      this if not set.
                    type: number
                  ipVersion:
                    description: 'The IP version to match: `BOTH`, `IPV4`, or `IPV6`.
                      Defaults to `IPV4`.'
                    type: string
                  logging:
                    description: Whether to log packets matching this policy. Defaults
                      to `false`.
                    type: boolean
                  name:
                    description: The name of the firewall policy.
                    type: string
                  protocol:
                    description: 'The protocol to match: `all`, `tcp`, `udp`, `tcp_udp`,
                      `icmp`, or `icmpv6`. Defaults to `all`. Note: for `icmp`/`icmpv6`
                      policies the controller rejects `create_allow_respond = true`
                      (`FirewallPolicyCreateRespondTrafficPolicyNotAllowed`) — keep
                      it `false` and add an explicit reverse policy if you need the
                      reply.'
                    type: string
                  site:
                    description: The name of the UniFi site. Defaults to the site
                      configured in the provider.
                    type: string
                  source:
                    properties:
                      clientMacs:
                        description: List of client MAC addresses to match. Used when
                          `matching_target` is `CLIENT`.
                        items:
                          type: string
                        type: array
                      ipGroupId:
                        description: ID of a `unifi_firewall_group` (address-group
                          type) to match. Used when `matching_target` is `IP` with
                          `matching_target_type = OBJECT`.
                        type: string
                      ips:
                        description: List of IP addresses or CIDR ranges to match.
                          Used when `matching_target` is `IP`.
                        items:
                          type: string
                        type: array
                      matchingTarget:
                        description: 'What to match: `ANY`, `NETWORK`, `CLIENT`, `IP`,
                          `DEVICE`, `MAC`, or `WEB` (domains/FQDN).'
                        type: string
                      matchingTargetType:
                        description: How the matching target is specified (`ANY`,
                          `SPECIFIC`, `LIST`, `OBJECT`). Managed by the UniFi controller;
                          the provider round-trips it so updates are accepted.
                        type: string
                      networkIds:
                        description: List of UniFi network IDs to match. Used when
                          `matching_target` is `NETWORK`.
                        items:
                          type: string
                        type: array
                      port:
                        description: Port(s) to match when `port_matching_type` is
                          `SPECIFIC`. A single port (`161`) or a comma-separated list
                          of ports/ranges (`80,443`, `8000-8100`). Leave unset for
                          no port match.
                        type: string
                      portGroupId:
                        description: ID of a `unifi_firewall_group` (port-group type)
                          to match. Used when `port_matching_type` is `OBJECT`.
                        type: string
                      portMatchingType:
                        description: 'How to match ports: `ANY`, `SPECIFIC`, or `OBJECT`
                          (port group).'
                        type: string
                      webDomains:
                        description: List of domains/FQDNs to match. Used when `matching_target`
                          is `WEB`.
                        items:
                          type: string
                        type: array
                      zoneId:
                        description: The ID of the firewall zone this endpoint belongs
                          to. Use the `unifi_firewall_zone` data source to look up
                          zone IDs by name.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                      delete:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Setting a timeout for a Delete operation is
                          only applicable if changes are saved into state before the
                          destroy operation occurs.
                        type: string
                      read:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours). Read operations occur during any refresh or
                          planning operation when refresh is enabled.
                        type: string
                      update:
                        description: A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration)
                          consisting of numbers and unit suffixes, such as "30s" or
                          "2h45m". Valid time units are "s" (seconds), "m" (minutes),
                          "h" (hours).
                        type: string
                    type: object
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
